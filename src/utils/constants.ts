export const MAX_SPEED = { x: 5, y: 5 };
export const SPEED = { x: 0, y: 0 };

export const SQUARE_SIZE = 50;
export const RADIUS = SQUARE_SIZE / 2;

export const CANVAS_SIZE = {
  WIDTH: window.innerWidth,
  HEIGHT: window.innerHeight,
};
